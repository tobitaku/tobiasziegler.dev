---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

<div class="flex items-center space-x-2">
  {
    tags.map((tag) => (
      <span class="rounded-md bg-indigo-900 bg-opacity-30 px-2 py-1">
        #{tag}
      </span>
    ))
  }
</div>
