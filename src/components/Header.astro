---
import type { Route } from '../types/Route';
import NavList from './NavList.astro';
const routes: Route[] = [
  {
    href: '/',
    name: 'Home',
    tab: false,
  },
  { href: '/blog', name: 'Blog', tab: false },
];
---

<header class="font-light text-sm text-zinc-400">
  <NavList routes={routes} />
</header>
